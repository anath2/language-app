[project]
name = "language-app-scripts"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "dspy>=2.5",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pyright>=1.1.408",
]
