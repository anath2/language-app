# Language App

This repository is split into two workspaces:

- `server/`: FastAPI backend, data pipeline, and tests.
- `web/`: Svelte + Vite frontend workspace.

Frontend checks:
- `cd web && npm run typecheck`
